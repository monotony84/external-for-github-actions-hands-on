name: Test
on: push

jobs:
  test:
    runs-on: ubuntu-latest
    name: Test
    steps:
      - name: Checkout
        uses: actions/Checkout@V2.0.0
      - name: Verify hello actions works
        uses: ./